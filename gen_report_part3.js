const { heading, para, bullet, pageBreak, emptyLine, makeTable } = require("./gen_report_part1");
const { HeadingLevel } = require("docx");

const ch5 = [
  heading("CHAPTER 5: IMPLEMENTATION"),

  heading("5.1 System Design and Architecture", HeadingLevel.HEADING_2),
  para("When we sat down to actually build FeedByMe, the first decision was how to organize the code. We went with a pattern that is pretty standard in the industry — separate the frontend and backend into their own folders, and within the backend, split things into models, controllers, routes, and middleware. This is sometimes called the MVC pattern, where M stands for Model (the data), V stands for View (the interface), and C stands for Controller (the logic in between)."),
  para("Our project folder has two main subfolders. The client folder contains everything related to the React frontend — the pages, components, context providers, utility functions, and styles. The server folder contains everything related to the Express backend — the models, controllers, routes, middleware, and the main server file. These two folders are basically two separate applications that talk to each other through HTTP requests."),
  para("Inside the client/src folder, we have five subfolders. The pages folder has sixteen files, one for each screen in the app — Dashboard, Login, Register, ProjectBoard, FeedbackNew, FeedbackDetail, and so on. The components folder has eleven reusable UI elements — Navbar, Sidebar, FeedbackCard, StarRating, FilterBar, etc. The context folder has four files for global state management. The utils folder has helper functions and the Axios API client. And the assets folder has static files like images."),
  para("Inside the server folder, we have four subfolders. The models folder has four Mongoose schema definitions — User, Project, Feedback, and Notification. The controllers folder has five files with all the business logic — auth, project, feedback, admin, and notification controllers. The routes folder has five files that map URL paths to controller functions. The middleware folder has three files — auth (JWT verification), errorHandler (global error catching), and validate (input validation rules)."),
  para("Communication between the frontend and backend happens through Axios. During development, Vite's proxy sends all requests starting with /api to the Express server on port 5000. In production on Vercel, the vercel.json config does the same routing. This means our frontend code always uses relative paths like /api/auth/login regardless of where the app is running."),

  heading("5.2 Workflow and Process Description", HeadingLevel.HEADING_2),
  para("Let me walk you through how someone actually uses FeedByMe from start to finish. This is the real workflow, not a theoretical diagram."),
  para("Step one — you open the landing page. You see a clean page that explains what FeedByMe does. There are two buttons — Sign In and Get Started. You click Get Started, which takes you to the register page."),
  para("Step two — you fill in your name, email, and password. You can toggle the password visibility with the eye icon to make sure you typed it right. You confirm the password in a second field. You click Create Account. Behind the scenes, React sends a POST request to /api/auth/register. The server checks if the email is already taken, hashes your password, creates your account in MongoDB, generates a JWT token, and sends it back. React stores the token in localStorage and redirects you to the dashboard. The whole thing takes about one second."),
  para("Step three — you are on the dashboard, which is empty because you have not created any boards yet. You click the New Form button. A modal pops up asking for a project name, description, theme color, and optional custom questions. Let us say you type My Website Feedback, pick a blue theme, and add a custom question: What browser are you using? with type Short Text and mark it as required. You click Save. The server creates a Project document and the board appears as a card on your dashboard."),
  para("Step four — you copy the shareable link by clicking the clipboard icon on the board card. The link looks like http://localhost:5173/board/abc123. You send this link to your friend, colleague, or customer."),
  para("Step five — your friend opens the link. They see the board page with the project name, your theme color, and a Submit Feedback button. They click it and see the feedback form. Since they are not logged in, the form shows an email field at the top. They enter their email, click three stars, type a title like Homepage loads slowly, describe the issue in the description box, type Chrome in the custom question field, maybe drag and drop a screenshot, and click Submit. The server saves this as a Feedback document linked to your project."),
  para("Step six — you go back to your dashboard, click on your board, and there it is — a new feedback entry from your friend. You can see their rating, their title, and their email. You click on it to see the full details including their description, screenshot, and custom answer. You can change the status from Open to In Progress to show that you are working on it. You can add a comment saying Thanks, I will look into this. You can even upvote the feedback if you think it is a valid concern."),
  para("Step seven — if you have an admin account, you can go to the Admin Dashboard and see analytics for the entire system. You see pie charts showing how many feedback entries are in each status, bar charts showing the category breakdown, and summary numbers for total users, total feedback, and average rating."),
  para("That is the complete workflow. It is designed to be as simple as possible. Create a board, share a link, get feedback, manage it. No complicated setup, no mandatory logins for your users, no paywall."),

  heading("5.3 Module Description", HeadingLevel.HEADING_2),
  para("Each module in FeedByMe is responsible for one specific area of functionality. Here is a detailed look at what each module does and how we built it."),

  heading("5.3.1 Authentication Module", HeadingLevel.HEADING_3),
  para("The authentication module handles everything related to identity — who are you, prove it, and what are you allowed to do. It is made up of the User model, the authController, the auth middleware, and four frontend pages (Register, Login, ForgotPassword, ResetPassword)."),
  para("The User model defines what a user looks like in the database. It has fields for name, email, password, role (user or admin), avatar URL, and password reset fields. The password field has a special setting — select: false — which means when you query users from the database, the password hash is not included by default. You have to explicitly ask for it. This prevents accidental leaks of password data in API responses."),
  para("There is a pre-save hook on the model that runs every time a user document is saved. It checks if the password field has been changed. If it has, it hashes the new password using bcrypt with 12 salt rounds. This means the controller code never has to worry about hashing — it just sets the password to whatever the user typed, and the model handles the rest. This is a nice pattern because it means you cannot accidentally forget to hash the password. It happens automatically."),
  para("The authController has six functions. The register function first checks if the email is already in use. If it is, it returns a 400 error. If not, it creates a new User document. The pre-save hook hashes the password. Then the controller generates a JWT token containing the user's ID and sends it back along with the user data (minus the password, thanks to our toJSON override)."),
  para("The login function finds the user by email with the password explicitly selected. It calls the comparePassword method on the user model, which uses bcrypt to compare the submitted password with the stored hash. If they match, it generates a JWT token. If they do not match, it returns a 401 error. It is important that the error message says Invalid credentials rather than Wrong password, because telling an attacker that the email exists but the password is wrong gives them information they should not have."),
  para("The forgotPassword function generates a random 20-byte token, hashes it with SHA-256, stores the hash and a 10-minute expiry timestamp on the user document, and logs the reset URL to the console. In a real production app, this would send an email instead. The resetPassword function takes the token from the URL, hashes it the same way, and looks for a user with a matching token that has not expired. If found, it updates the password and clears the reset fields."),
  para("The auth middleware is a function that runs before protected route handlers. It reads the JWT from the Authorization header, verifies it using the secret key from the environment variables, extracts the user ID, looks up the user in the database, and attaches the user object to req.user. If any step fails, it sends a 401 response. We also have an optionalAuth variant that does all of this but does not reject the request if there is no token — it just sets req.user to null and moves on. This is used for endpoints like feedback submission where both logged-in users and guests are welcome."),
  para("On the frontend, the Register and Login pages share the same two-panel layout. The left panel has branding — the FeedByMe logo and some stats. The right panel has the form. Both pages include a password visibility toggle (eye icon) that switches the input type between password and text. The AuthContext provider wraps the entire app and exposes the current user, login/register functions, and logout function to any component that needs them."),

  heading("5.3.2 Project Management Module", HeadingLevel.HEADING_3),
  para("The project management module handles the creation and lifecycle of feedback boards. It consists of the Project model, the projectController, and the Dashboard page on the frontend."),
  para("The Project model stores the board name, description, owner (a reference to the Users collection), a visibility flag, a theme color (defaulting to government blue), a logo URL, and an array of custom field definitions. Each custom field is an embedded object with a label (the question text), a fieldType (text, textarea, or rating), and a required boolean. The model also has a virtual property that references all feedback documents linked to this project, which is useful for counting feedback without running a separate query."),
  para("The projectController has five functions. getProjects is interesting because it does not just return projects you own — it also returns projects where you have submitted feedback. It does this by first querying the Feedbacks collection for entries authored by the current user, extracting the project IDs, and then querying the Projects collection with an OR condition: owner is you OR _id is in the list of project IDs from your feedback. This way, users see both their own boards and boards they have contributed to."),
  para("createProject takes the submitted data, sets the owner to the current user, and creates the document. getProject returns a single project by ID and is publicly accessible so that anyone with the board link can see the project info. updateProject and deleteProject both check that the current user is the project owner before making changes. When a project is deleted, the controller also deletes all feedback entries linked to that project — you do not want orphaned feedback floating around in the database."),
  para("The Dashboard page displays project cards in a responsive grid. Each card shows a colored stripe at the top (using the theme color), the project name, description, and four action buttons. The copy link button uses the browser's clipboard API to copy the board URL. The edit button opens a modal where you can change all project settings including the custom questions. The custom question builder is a dynamic form — you can add new questions, remove existing ones, change their types, and toggle the required flag. All changes are saved when you click the Update button."),

  heading("5.3.3 Feedback Core Module", HeadingLevel.HEADING_3),
  para("This is the biggest and most important module. It handles everything related to feedback — submitting it, viewing it, filtering it, upvoting it, commenting on it, and deleting it. The backend consists of the Feedback model, the feedbackController, and the feedback routes. The frontend includes FeedbackNew, FeedbackDetail, FeedbackEdit, ProjectBoard, and the FeedbackCard component."),
  para("The Feedback model is the most complex schema in the app. It has sixteen fields: project (reference to Projects), title, description, category (an enum with five options), status (an enum with five options), priority (an enum with four options), rating (number, 1 to 5), tags (array of strings), author (optional reference to Users), guestEmail, isAnonymous, image (file path), assignedTo (reference to Users), upvotes (array of User references), comments (embedded subdocuments), and customAnswers (array of label-value pairs)."),
  para("The schema also defines five database indexes. A single-field index on project for fast board lookups. A compound text index on title, description, and tags for full-text search. A compound index on status, category, and priority for efficient filtering. A single-field index on author for user-specific queries. And a descending index on createdAt for sorting by newest first. These indexes are important for performance — without them, MongoDB would have to scan every document in the collection for every query."),
  para("The feedbackController has seven functions. getAllFeedback is the most complex. It accepts query parameters for projectId, status, category, priority, author, search text, sort field, sort direction, page number, and items per page. It builds a query object dynamically based on which parameters are present. If you filter by status=Open and category=Bug, the query object becomes { status: Open, category: Bug }. If you also search for login error, it adds a text search condition. The results are sorted, paginated with skip() and limit(), and returned with total count and total pages so the frontend can render pagination controls."),
  para("createFeedback handles both authenticated and guest submissions. It reads the form data from the request body. If there is a file uploaded via Multer, it saves the file path to the image field. If the user is authenticated (req.user exists), it sets the author field. If not, it uses the guestEmail from the form. Custom answers are parsed from the request body — they come as a JSON string that needs to be parsed into an array. The function creates the Feedback document and returns it."),
  para("upvoteFeedback uses a toggle pattern. It checks if the current user's ID is already in the upvotes array. If it is, the user is removing their upvote, so we use MongoDB's pull operator to remove the ID. If it is not, the user is adding their upvote, so we use addToSet to add the ID. addToSet is better than push because it prevents duplicates — if someone somehow sends the upvote request twice, their ID will only appear once."),
  para("addComment pushes a new object into the comments array with the user's ID, the comment text, and the current timestamp. deleteFeedback runs three authorization checks: is the requester the original author, is the requester the board owner, or is the requester an admin? Only if one of these is true does the deletion proceed."),
  para("On the frontend, FeedbackNew is the submission form. It fetches the project data to get the custom field definitions, then renders the form with the default fields plus any custom questions. The star rating uses a clickable star component — you hover over stars and they light up, and clicking sets the rating. The image upload area supports both click-to-browse and drag-and-drop. When you drop a file, a preview appears. The whole form is submitted as FormData (not JSON) because we need to include the binary image file."),
  para("ProjectBoard is the listing page. It shows all feedback for a specific board with the FilterBar component at the top. The FilterBar has three dropdowns (status, category, priority) and a text search input. When you change any filter, the page refetches feedback from the API with the updated parameters. Each feedback entry is rendered as a FeedbackCard component showing the title, star rating, author name or guest email, date, and status badge."),
  para("FeedbackDetail shows the full feedback entry with all its data, including the image, custom answers, comments, and upvote button. Board owners see a status dropdown that lets them change the status. Authenticated users see a comment input field. The upvote button toggles between upvoted and not-upvoted states."),

  heading("5.3.4 Analytics and Dashboard Module", HeadingLevel.HEADING_3),
  para("The analytics module gives admins a bird's eye view of the entire system. It consists of the adminController on the backend and the AdminDashboard and AdminUsers pages on the frontend."),
  para("The adminController has a getDashboardStats function that runs several database queries. It counts total users, counts total feedback entries, calculates the average rating across all feedback, groups feedback by status (counting how many are Open, In Progress, Under Review, Resolved, and Closed), and groups feedback by category (counting Bugs, Feature Requests, Improvements, etc.). All these numbers are sent to the frontend in a single API response."),
  para("The AdminDashboard page takes these numbers and turns them into something visual. At the top, three summary cards show total users, total feedback, and average rating. Below that, a Recharts PieChart shows the status distribution — each slice is a different color, and hovering over a slice shows the exact count. Next to it, a Recharts BarChart shows the category distribution with one bar per category. These charts update every time the page loads, so the admin always sees current data."),
  para("The AdminUsers page shows a paginated table of all registered users. Each row shows the user's name, email, role, and registration date. This gives the admin visibility into who is using the platform and when they signed up."),

  heading("5.4 System Integration", HeadingLevel.HEADING_2),
  para("All four modules work together through shared state and consistent API design. The AuthContext is consumed by every other module — the project controller checks req.user to know who is creating a board, the feedback controller checks req.user to know who is submitting or deleting feedback, and the admin routes check if req.user.role is admin before allowing access."),
  para("The routing system on the frontend ties everything together. React Router defines which page component renders for which URL. The route wrappers enforce access control — ProtectedRoute redirects to login if you are not authenticated, AdminRoute redirects to the dashboard if you are not an admin, and GuestRoute redirects to the dashboard if you are already logged in (so you do not see the login page when you are already logged in)."),
  para("On the backend, Express middleware provides cross-cutting functionality. CORS, Helmet, rate limiting, body parsing, and error handling are configured once in server.js and apply to every request. This means security and validation are handled consistently without any duplication."),

  heading("5.5 Advantages of Implementation", HeadingLevel.HEADING_2),
  para("Here are the concrete advantages of how we built FeedByMe:"),
  bullet("One language everywhere — JavaScript for frontend, backend, database, and tools. No language switching."),
  bullet("Reusable components — StarRating, FeedbackCard, FilterBar, and ConfirmModal are written once and used in many places."),
  bullet("Flexible database — MongoDB let us add custom fields, guest emails, and image paths without restructuring existing data."),
  bullet("Stateless auth — JWT tokens mean any server instance can handle any request. Good for scaling."),
  bullet("Serverless ready — the architecture works on Vercel without any modifications. Database connections are handled per-request."),
  bullet("Zero cost — MongoDB Atlas free tier plus Vercel free tier equals zero rupees for hosting."),
  bullet("Clean code organization — models, controllers, routes, and middleware have clear separation of responsibilities."),

  heading("5.6 Summary", HeadingLevel.HEADING_2),
  para("Building FeedByMe was a hands-on exercise in full-stack development. We designed the database schemas, built the REST API with authentication and file uploads, created a responsive frontend with sixteen pages and eleven components, and wired everything together with context-based state management. Each module was built incrementally — we would build the model first, then the controller, then the routes, then the frontend page, and test the entire flow before moving to the next module. This incremental approach meant we always had a working application, even while new features were still being added."),
  pageBreak(),
];

const ch6 = [
  heading("CHAPTER 6: FUTURE SCOPE AND CONCLUSION"),

  heading("6.1 Future Scope", HeadingLevel.HEADING_2),
  para("FeedByMe does what it was designed to do, and it does it well. But there is always room to grow. Software is never really finished — you just ship a version and start thinking about the next one. Here are the things we would work on if we continued developing FeedByMe."),
  para("The number one priority would be email integration. Right now, when you use the forgot password feature, the reset link gets printed to the server console. That is fine for development, but it is useless in production. You cannot ask your users to open the server terminal to get their reset link. We would integrate a service like SendGrid or Mailgun to send actual emails. This would also let us send notification emails — like when someone submits feedback on your board, you would get an email about it instead of having to check the dashboard manually."),
  para("The second priority would be cloud image storage. Currently, uploaded screenshots are saved to the server's local filesystem. This works perfectly on localhost, but on Vercel, the filesystem is temporary. Files disappear after the serverless function shuts down. The fix is to use a service like Cloudinary or Amazon S3 to store images. Cloudinary has a generous free tier and also gives you automatic image resizing and format optimization, which would make the app faster for users on slow connections."),
  para("Real-time updates would make a huge difference in user experience. Right now, if you and your colleague are both looking at the same board and your colleague submits new feedback, you have to refresh the page to see it. With WebSockets (using a library like Socket.io), the server could push the new feedback to your browser instantly. Comments, upvotes, and status changes would all appear in real time without any page refresh."),
  para("The admin dashboard could be much more powerful. Currently it shows total numbers and two charts. We would add date-range filtering so you can see feedback from this week, this month, or a custom range. We would add trend lines showing how feedback volume changes over time. We would add the ability to export data as CSV files so you can open them in Excel. And we would add a simple sentiment analysis that classifies feedback text as positive, negative, or neutral."),
  para("Team collaboration is another big feature we would want. Right now, only the board owner can manage feedback. But in a real team, you want multiple people to be able to respond to feedback, assign it to each other, and update statuses. We would add an invite system where the board owner can invite team members with specific roles — like viewer (can only read), editor (can comment and update status), or manager (can do everything including delete)."),
  para("Other features on our wishlist include: email verification during registration (to prevent fake accounts), a dark mode toggle (our CSS already uses variables, so this would be straightforward), multi-language support using react-i18n, webhook integrations so you can get Slack or Discord notifications when new feedback comes in, a public API with documentation for third-party developers, and a mobile app built with React Native that reuses our existing backend API."),
  para("We would also want to improve the search functionality. Right now, the text search uses MongoDB's built-in text indexes, which are decent but not great. They do not handle typos, partial matches, or synonyms well. Integrating a proper search engine like Elasticsearch or Algolia would give users much better search results."),
  para("Finally, we would add an audit log that tracks every action performed in the system — who created what, who deleted what, who changed what status, and when. This is important for accountability and debugging. If a feedback entry goes missing, the audit log would show exactly who deleted it and when."),

  heading("6.2 Conclusion", HeadingLevel.HEADING_2),
  para("When we started this project, all we had was a simple idea: make it easy for people to tell you what they think. Over the weeks that followed, that idea turned into a real application with real features that solve real problems. We built user authentication from scratch. We designed a database that handles nested data like comments, custom answers, and upvote arrays. We created sixteen pages with responsive layouts that work on everything from a phone to a widescreen monitor. We set up deployment on a cloud platform. And we did all of it using free, open-source tools."),
  para("The MERN stack turned out to be the right choice for this project. MongoDB's document model was a natural fit for our data — feedback entries are complex objects with nested arrays and optional fields, and MongoDB handles that effortlessly. Express gave us a clean, minimal framework that let us focus on logic instead of boilerplate. React's component model made the frontend organized and efficient. And Node.js tied everything together with JavaScript from top to bottom."),
  para("We learned things that textbooks cannot teach. We learned that CORS errors will make you question your life choices until you understand that the browser is just trying to protect users. We learned that forgetting to add await before a database call creates bugs that are incredibly hard to track down because the code does not crash — it just returns a Promise instead of the actual data. We learned that mobile responsive design is not just about adding media queries — you have to think about tap targets, font sizes that do not trigger iOS zoom, and landscape versus portrait orientations."),
  para("We learned about deployment the hard way. Our app worked perfectly on localhost. Then we deployed to Vercel and nothing worked. The database connection timed out because Vercel's IP was not whitelisted. The file uploads broke because Vercel's filesystem is ephemeral. The CORS config that accepted localhost did not accept the Vercel domain. Each problem taught us something about how production environments differ from development environments."),
  para("We also learned about the importance of validation. Early in development, we had a bug where someone could submit feedback without a title because we only validated on the frontend. A direct API call with an empty title went right through. That is when we added server-side validation with express-validator and schema-level validation with Mongoose. Three layers of defense. Now bad data simply cannot get into the database."),
  para("Looking at the finished product, we are genuinely happy with how it turned out. The UI is clean and professional. The features work as expected. The code is organized and readable. The app is deployed and accessible from anywhere. It is not perfect — we have honestly listed its limitations — but it does exactly what we set out to do. It lets anyone create a feedback board, share a link, and start collecting organized, actionable feedback. No cost, no complexity, no barriers."),
  para("This project was more than just a college assignment. It was the closest thing to real software development that we have experienced. We went through the same stages that professional developers go through — understanding the problem, designing the solution, implementing it piece by piece, testing it, fixing bugs, deploying it, and then fixing the new bugs that deployment introduced. It was challenging, frustrating at times, and incredibly satisfying when things finally worked."),
  para("FeedByMe is our contribution to making feedback collection accessible to everyone. We hope it is useful, and we hope this report gives a clear, honest picture of how we built it."),
  pageBreak(),
];

const ch7 = [
  heading("CHAPTER 7: BIBLIOGRAPHY"),
  emptyLine(),
  para("1. MongoDB Inc., MongoDB Documentation, Available at: https://docs.mongodb.com/ [Accessed: April 2026]"),
  para("2. Meta Platforms Inc., React Documentation, Available at: https://react.dev/ [Accessed: April 2026]"),
  para("3. OpenJS Foundation, Express.js Documentation, Available at: https://expressjs.com/ [Accessed: April 2026]"),
  para("4. OpenJS Foundation, Node.js Documentation, Available at: https://nodejs.org/docs/ [Accessed: April 2026]"),
  para("5. Auth0, JSON Web Tokens Introduction, Available at: https://jwt.io/introduction/ [Accessed: April 2026]"),
  para("6. Evan You, Vite Documentation, Available at: https://vitejs.dev/ [Accessed: April 2026]"),
  para("7. Automattic, Mongoose ODM Documentation, Available at: https://mongoosejs.com/ [Accessed: April 2026]"),
  para("8. Express.js Contributors, Multer — File Upload Middleware, Available at: https://github.com/expressjs/multer [Accessed: April 2026]"),
  para("9. Recharts Contributors, Recharts Charting Library, Available at: https://recharts.org/ [Accessed: April 2026]"),
  para("10. D. Arias, bcryptjs — Password Hashing Library, Available at: https://www.npmjs.com/package/bcryptjs [Accessed: April 2026]"),
  para("11. M. Zabriskie, Axios — Promise Based HTTP Client, Available at: https://axios-http.com/ [Accessed: April 2026]"),
  para("12. Remix Software, React Router Documentation, Available at: https://reactrouter.com/ [Accessed: April 2026]"),
  para("13. OWASP Foundation, OWASP Top Ten Web Security Risks, Available at: https://owasp.org/www-project-top-ten/ [Accessed: April 2026]"),
  para("14. W3C, Web Content Accessibility Guidelines (WCAG) 2.1, Available at: https://www.w3.org/TR/WCAG21/ [Accessed: April 2026]"),
  para("15. E. Hahn, Helmet.js — Security Middleware for Express, Available at: https://helmetjs.github.io/ [Accessed: April 2026]"),
  para("16. N. Persaud, express-rate-limit, Available at: https://www.npmjs.com/package/express-rate-limit [Accessed: April 2026]"),
  para("17. express-validator Contributors, express-validator Documentation, Available at: https://express-validator.github.io/ [Accessed: April 2026]"),
  para("18. MongoDB Inc., MongoDB Atlas Cloud Database Service, Available at: https://www.mongodb.com/atlas [Accessed: April 2026]"),
  para("19. Vercel Inc., Vercel Serverless Platform Documentation, Available at: https://vercel.com/docs [Accessed: April 2026]"),
  para("20. T. Kroes, React Hot Toast — Notifications for React, Available at: https://react-hot-toast.com/ [Accessed: April 2026]"),
  pageBreak(),
];

module.exports = { ch5, ch6, ch7 };
